# rational-docs
Rational Docs

Verify the extension packaging example with `node scripts/test-extension-packaging.mjs`
(Node.js and Python 3). The check runs the documented script and inspects the
resulting archives, including changed identities and invalid inputs.
